const draw = (context, canvas, pondData) => {
  // context.arc(
  //   canvas.width / 2,
  //   canvas.height / 2,
  //   700 * Math.abs(Math.cos(2)),
  //   0,
  //   2 * Math.PI
  // );
  // context.fill();
};

export default draw;